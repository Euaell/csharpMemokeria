List<char> even = new List<char>();
                List<char> odd = new List<char>();
                for (int i = 0; i < s.Count; i++)
                {
                    if (i % 2 == 0)
                    {
                        even.Add(s[i]);
                    }
                    else
                    {
                        odd.Add(s[i]);
                    }
                }
                